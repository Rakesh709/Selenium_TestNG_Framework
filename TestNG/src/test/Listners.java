package test;

import org.testng.ITestListener;

//import org.testng.ITestListener;

//import ITestListener(org.testng) 
//
//import org.testng.ITestListener;

//ITestListner interface which implement testng listners

public class Listners implements ITestListener {
		// not able to implement the methods automatic so leave it blank as of now.
}
 